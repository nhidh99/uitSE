import styled from "styled-components";

const EmptyContainer = styled.div`
    margin-top: 15px;
    border: 1px dashed #bbb;
    padding: 20px 0;
    border-radius: 5px;
`;

export const SC = { EmptyContainer };
