import styled from "styled-components";

const Container = styled.div`
    display: flex;
    gap: 20px;
`;

const LeftContainer = styled.div`
    flex: 1;
`;

const RightContainer = styled.div`
    flex: 4;
`;

export const SC = { Container, LeftContainer, RightContainer };
