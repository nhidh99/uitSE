import styled from "styled-components";

const Container = styled.div`
    display: flex;
    align-items: center;
`;

const QuantityLabel = styled.label`
    font-weight: bold;
    margin-right: 15px;
`;

export const SC = {
    Container,
    QuantityLabel,
};
