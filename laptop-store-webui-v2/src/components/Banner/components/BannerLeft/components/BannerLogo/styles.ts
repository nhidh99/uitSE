import styled from "styled-components";

const Label = styled.label`
    margin-bottom: 0;
    margin-left: 8px;
    font-size: 16px;
    font-weight: bold;
    color: white;
    cursor: pointer;
`;

const Container = styled.div`
    display: flex;
    align-items: center;
    cursor: pointer;
`;

export const SC = {
    Container,
    Label,
};
