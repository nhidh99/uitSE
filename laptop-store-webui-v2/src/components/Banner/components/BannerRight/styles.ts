import styled from "styled-components";

const NavBar = styled.nav`
    ul {
        display: inline-block;
        margin: 0;
    }

    li {
        padding: 0 10px;
        display: inline-block;
        color: white;
        text-align: center;
        cursor: pointer;
    }
`;

export const SC = {
    NavBar,
};
